services:
  shortmaker:
    build:
      context: .
    ports:
      - "8000:8000"
    env_file:
      - .env.production
    volumes:
      - ./outputs:/app/outputs
    environment:
      - SHORTMAKER_AUTH_MODE=production
